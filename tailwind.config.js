export const content = ['./src/**/*.{svelte,js,ts}'];
export const plugins = [require('daisyui')];
export const daisyui = {
    // first theme in this list is the default theme
    themes: ["dark"],
};
